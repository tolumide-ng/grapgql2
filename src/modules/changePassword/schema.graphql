type ChangePasswordResponse {
    status: String!,
    message: String!
}

type Query {
    changePwd: String!
}


type Mutation {
    changePassword(passwordResetLink: String!, password: String!): ChangePasswordResponse!
}